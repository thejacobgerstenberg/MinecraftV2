# FINDINGS.md — UI integration TODO / risk register

Severity/owner table from integrating the UI bundle (ui-kit #10 + brand #9 +
ux-access #17) against the builder's real HUD. Everything below is code-verified
against `ui-integrate/vendor-game/` (read-only HUD copy) and
`ui-integrate/vendor-ux/` (read-only ux-access copy).

| # | Finding | Severity | Owner | Detail | Action |
|---|---------|----------|-------|--------|--------|
| 1 | Captions need the builder to emit `lf-audio-event` | **HIGH** | builder | `<lf-captions>` self-subscribes to window `lf-audio-event` `{name, direction?, volume?, loop?, ended?}` on connect — there is **no `attach()`**. Nothing is emitted by the kit. | In `GameAudio`, dispatch `lf-audio-event` per `engine.play`: compute `direction` from sound pos vs. the listener set in `setListener`; `startMusic -> music.<mode>`; a looping sound stopping emits `{name, ended:true}`. Exact code in INTEGRATION section 3.2. Without it the caption layer stays silent. |
| 2 | `settings-shell.css` lacks a narrow-width breakpoint | **MED** | ui-kit | Rail pinned `flex: 0 0 168px`, so the pane collapses to ~124px at 390w (dropdown ~101px). | Shipped as overlay `ui-integrate/settings-shell-fix.css` (`@media max-width:440px` stacks the rail; linked LAST by `applyTheme`). **Verified 124px -> 308px, dropdown 101px -> 285px.** Upstream should fold the `@media` into `settings-shell.css`. |
| 3 | No 2.60/5 doc exists in the repo | **NOTE** | coordinator / QA | Searched all branches — there is **no** 2.60/5 scorecard. The only committed scorecard is the rubric's worked-example **3.45/5 FAIL** (R1=2 from `#FFFFFF` on `#C6C6C6` creative tabs). | Treat the measured baseline on the REAL current HUD as authoritative: **3.80 (before) -> 4.35 (after)**. R2/R3/R5/R7 are HELD (not moved by a restyle). |
| 4 | R2/R3/R5/R7 are not moved by a UI restyle | **MED** | builder | A theme/component swap lifts R1 (contrast) and R4 (palette) but cannot move layout/feedback/material criteria. | To reach a full 5/5: **R2** widget layout within +/-2px at all gui scales; **R7** feedback affordances (hover label `#FFFFA0`, break-crack stage scaling >=1.5x, rarity-colored item popup); **R5** world-material distinctness. Out of scope for this bundle. |
| 5 | `tutorial.json` `gateEvent` uses the `on_` prefix | **LOW** | ux-access | README prose omits the prefix; the JSON gates are `on_world_start`, `on_first_block_broken`, etc. Beats also match on `beat.id`. | Dispatch `lf-game-event` with the `on_`-prefixed `type` (or rely on the `b.id` fallback). Documented in INTEGRATION section 3.3. |
| 6 | lf HUD widgets don't self-position | **LOW** | builder (adoption) | `lf-hotbar`/`lf-statbars`/`lf-crosshair`/`lf-chat` render styling but do not own placement — the game shell owns HUD layout. | Place adopted widgets exactly where the bespoke widgets sat; `demo.html` shows the anchoring CSS. |
| 7 | OptionsStore shares `localStorage['loomfall.settings']` with `menu.js` | **LOW** (compatible) | builder | `OptionsStore` flattens `volumes.* -> volumeMaster/Sfx/Music` and `fov -> fov`, and **preserves** other keys written by `menu.js` SETTINGS_SPEC. | Single source of truth is good — just note the overlap. No migration needed; both read/write the same key safely. |
| 8 | Asset-path split (ui-kit at repo root vs. theme css beside the page) | **LOW** (resolved) | — | `ui-kit/` lands at the game repo root, but `brand-theme.css` / `settings-shell-fix.css` live beside `integrate.js`. | Handled by constructor opts `themeHref` / `settingsFixHref` (and `wordmarkHref`), which pin those files independently of `assetBase`. Defaults resolve module-relative. |

# Loomfall — Content Integration Findings

Generated 2026-07-11 from `coverage.json` (proof run 2026-07-11T20:02:11Z), the canon↔builder
stale-id diff, and the verifier's notes. Companion docs: `INTEGRATION.md` (wiring guide),
`LOADER_API.md` (frozen loader surface), `proof.mjs` (reproducible proof harness).

## Headline coverage

| Metric | Value |
|---|---|
| Pack load | **ok** — 15/15 files loaded, 0 failed (12 content JSONs + captions/bindings/tutorial) |
| Cross-reference checks | **818 / 820 resolved — 99.8%** |
| Systems at 100% | 13 of 14 (pack-load, achievements, recipes, mob-drops, structures, bosses, dialogue-telegraphs, tutorial, deathmessages, captions, bindings, consumer-stubs, stale-verify) |
| Only gap | books: 39/41 (95.1%) — no book authored for `deepest_spindle` or `selvage_outpost` |
| Stale/missing content ids in builder snapshot | 22 diff entries (2 whole files missing, 2 code-level drifts) |

**Blockers: 2 · Majors: 6 · Minors: 5 · Info: 3**

## Findings

| # | Severity | Finding | Evidence (file / id) | Owner | Suggested action |
|---|---|---|---|---|---|
| 1 | **blocker** | Last Needle becomes unsummonable if canon `boss.json` is adopted as-is: `summonRequirement` changed from `kill_count:selvage_warden:4` to `all_of[seal_released:seal_measured…seal_sewn]`, and the four seals are bound to the new `selvage_outpost` structure — but the live generator (`public/src/world/TerrainGenerator.js`) places **no structures at all**; `structures.json` has zero consumers. | `boss.json` (`last_needle.gauntlet`, `summonRequirement`); `structures.json` (`selvage_outpost`); coverage.json staleIds | builder (worldgen) + mobs team | Land the seal gauntlet as one unit: worldgen placement of the four named outposts + seal-holder spawn tags + `seal_released` event, per INTEGRATION.md §5c/§6. Until then, pin the old `kill_count` gate behind a flag — do not merge the new boss.json summon gate without the structures. |
| 2 | **blocker** | Second boss is off-canon: canon `molthkin.json` (3 phases, own gauntlet, arena `deepest_spindle`, hp 280 dmg 11 spd 2) is entirely missing from the builder snapshot; `MobManager` inlines a simplified 2-phase model (`phase1HpFrac 0.5`), and the `dialogue.json` molthkin actor (phaseIntros / attackTelegraphs / onFelled) plus the `deepest_spindle` arena structure are also absent. | `molthkin.json` (missing-in-builder); `MobManager.js` ARCHETYPE_CONFIG molthkin; `dialogue.json` molthkin actor; `structures.json` `deepest_spindle` | mobs team + builder | Refresh snapshot to pull molthkin.json; drive phase thresholds/intros from `pack.boss('molthkin')` + `dialogue('molthkin', …)` instead of the inline 2-phase config (INTEGRATION.md §5b); worldgen must place `deepest_spindle`. |
| 3 | major | Sneak/sprint keys are swapped relative to canon: ux `bindings.default.json` (and the fixed GAME_GUIDE.md controls table) say sneak=`ShiftLeft`, sprint=`ControlLeft`; builder `Controls.js` hardcodes sprint=`ShiftLeft`, sneak=`KeyC`/`ControlLeft`. Once the guide snapshot refreshes, in-game docs contradict actual controls. | `ux/bindings.default.json` vs `public/src/gameplay/Controls.js` L171-235; coverage.json notes | builder | Adopt canon bindings via `pack.binding(action)` (INTEGRATION.md §7); swap the two keys in the same change that refreshes GAME_GUIDE.md. |
| 4 | major | Input layer can't consume `bindings.json`: `Controls.js` hardcodes `e.code` switches with no rebinding layer, and 2 of 27 ux actions have no handler at all (`interact`, `eat`) — no gamepad support either. | `Controls.js` `_handleKeyDown`/`_handleKeyUp`; coverage.json notes ("ux-only actions … [interact, eat]") | builder | Introduce a code→action lookup built from `pack.bindings()` with the current hardcodes as fallback; stub `interact`/`eat` events even before gameplay consumes them. |
| 5 | major | All 17 mob stat blocks are hardcoded in `MobManager.js` ARCHETYPE_CONFIG, duplicating `bestiary.json` with `// canon:` comments — drift is already real (husbandry fields, molthkin phases) and undetectable at runtime. | `public/mobs/MobManager.js` L209-521 | mobs team | Add the drift guard from INTEGRATION.md §4a (dev-mode assert config vs `pack.bestiary(id).stats`), then migrate stats to read from the pack. |
| 6 | major | Only 29/60 achievements can ever fire: `parseTrigger()` wires 7 trigger families; craft/tame/breed/seal/anchor/bind triggers are silently unwired, and the new husbandry + seal content adds more permanently-dead achievements. | `public/src/systems/achievements.js` L96-137; coverage.json notes (stats {total:60, wired:29}) | builder | Emit the missing bus events (`item:crafted`, `mob:tamed`, `mob:bred`, `seal:released`, …) as their systems land; surface `stats().wired` in the debug overlay so the gap stays visible. |
| 7 | major | 7 of 21 authored death causes are unreachable: `main.js killPlayer()` only ever produces `void_unravel` and `mob:<canonicalId>` — `fall`/`lava`/`drowning`/`explosion`/`starvation`/`fire`/`cinderloom_heat` messages are dead content. | `deathmessages.json`; `main.js` L959/L1054-1056; coverage.json notes | builder | Wire environmental damage sources to `killPlayer(cause)` with the canon cause ids as each hazard system lands (INTEGRATION.md §2). |
| 8 | major | New husbandry mechanics have data but no engine: skeinling/bobbin_deer gained `breedable` + breeding foods, spoolmare gained `tameable`/`tameFood:raw_skein`/`mountRequires:saddle_frame`, and the new `sennit_grain` item (41st) exists only in canon — builder has no breed/tame system (only `mountPlayer()`), and its items.json snapshot has 40/41. | `bestiary.json` husbandry fields; `items.json` `sennit_grain`, `saddle_frame` note; coverage.json staleIds | mobs team + content team | Refresh snapshot so the data ships; flag husbandry as data-only in-game (don't fake it) per INTEGRATION.md §4b; mobs team schedules breed/tame engine work; gate `mountPlayer` on `mountRequires`. |
| 9 | minor | Boss defeat chat line is hardcoded ('The Last Needle is bound…', `main.js` L1063) instead of reading `dialogue.json` `lastNeedle.onBound`. | main.js L1063; coverage.json staleIds (stale-code) | builder | Replace with `pack.dialogueLine('lastNeedle','onBound')` — smallest win, do first (INTEGRATION.md §5a). |
| 10 | minor | Two new structures have no book placement: `book('deepest_spindle')` and `book('selvage_outpost')` return null — the only 2 unresolved checks in the proof (books 39/41). | coverage.json books.unresolved; `books.json` placements | content team | Either author a book placed in each (both are lore-heavy gauntlet/arena sites) or confirm intentionally bookless. |
| 11 | minor | Text-only staleness in builder snapshot (fixed by a snapshot refresh, no code impact): achievement `stitch_ground_beneath_hooves` renamed; 1 splash + 1 tip + naming.json spoolmare description rewritten to six-legged strider flavor; `warning_the_water_that_was` / `hymn_the_drumming_song` book text; 4 structure descriptions; GAME_GUIDE.md spoolmare sections; boss.md gauntlet paragraph. | coverage.json staleIds (achievements/splashes/tips/naming/books/structures/GAME_GUIDE/boss.md) | content team → builder | Run the snapshot refresh in INTEGRATION.md §0; no per-file action needed. |
| 12 | minor | `captions.json` defines 6 `boss.*.telegraph`/`boss.*.defeated` caption keys the audio engine never synthesizes — captions can't fire without the sounds. | `ux/captions.json` boss.* entries vs 66-key sfx registry (`public/audio/sfx/index.js`) | audio team + builder | Add boss telegraph/defeat sound keys to the sfx registry and play them from boss phase/defeat handlers; tap `GameAudio.play()` (L161) for the caption hook (INTEGRATION.md §8). |
| 13 | minor | Mob voice coverage gap: `VOICE_FAMILY` (main.js L103) folds all 17 archetypes onto 5 voiced families (grazer/groaner/exploder/screecher/trader) — 12 archetypes reuse another mob's hurt/death voice, and captions will name the wrong mob unless `{mob}` substitution uses the real archetype. | main.js L103; `mob.*.{idle,hurt,death}` sfx keys | audio team | Author per-archetype voices over time; short-term, pass the true canonical id into `captionText(key, {mob})` so captions stay accurate. |
| 14 | info | Loader proof green: `ContentPack.load()` resolves 15/15 files, all getters synchronous/deep-frozen/non-throwing; 820-check cross-reference proof at 99.8% with consumer-stub and stale-verify suites at 100%. | coverage.json totals/systems; `loader.js`, `loader.test.mjs`, `proof.mjs` | — | None — baseline for regression (re-run `node proof.mjs`). |
| 15 | info | `deathmessages.json`, `ending.md`, `lore.md`, `onboarding.md` are byte-identical between canon and builder snapshot — no action on refresh. | snapshot diff | — | None. |
| 16 | info | `mobs/integration/gameBridge.js` (`createMobBridge`) is an unused example adapter — not a live consumer; don't count it as coverage. | `public/mobs/integration/gameBridge.js` L58 | mobs team | Delete or mark as example to avoid confusion during wiring. |

## Reading the table

- **blocker** — merging the canon content as-is breaks progression or ships a boss that contradicts its own encounter file; must be resolved (or explicitly gated) before the snapshot refresh lands.
- **major** — user-visible wrongness or permanently dead content if unaddressed; safe to merge but must be scheduled.
- **minor** — cosmetic drift, dead caption keys, or single-line fixes.
- **info** — verified-green baselines and no-op notes.

Suggested landing order (mirrors INTEGRATION.md "Ordering + dependencies"): snapshot refresh (§0, clears #11 and the data halves of #2/#8) → defeat line #9 → bindings #3/#4 → stat drift guard #5 → death causes #7 → achievements events #6 → seal gauntlet + worldgen #1/#2 as one unit → captions/audio #12/#13.
